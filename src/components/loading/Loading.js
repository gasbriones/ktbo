import React from 'react'
import './Loading.css'

const Loading = () => (
    <div className={'loading'}>
        <h3 className="text-center message">Cargando...</h3>
    </div>
);

export default Loading;
